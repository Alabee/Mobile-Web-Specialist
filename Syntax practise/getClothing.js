function getClothing(isCold){
    var freezing, hot;
    isCold = true;
    if (isCold){
        freezing = 'Grab a jacket!';
        console.log(freezing);
    } else {
        hot = "It's a shorts kind of day.";
        console.log(hot);
    }
    
}